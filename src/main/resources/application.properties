# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/smart_commerce
spring.datasource.username=root
spring.datasource.password=1234

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Jackson Configuration
spring.jackson.serialization.indent-output=true

management.endpoints.web.exposure.include=health,info,metrics

#Redis Cache
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}
spring.data.redis.password=${REDIS_PASSWORD}

# Reveal Cache Hits and Misses
logging.level.org.springframework.cache=TRACE

# Secret
app.jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
app.jwt.expiration-ms=3600000

# --- Apache Kafka Configuration (Local Docker) ---
spring.kafka.bootstrap-servers=localhost:9092

# JSON Serialization Rules
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.consumer.group-id=notification-service-group
spring.kafka.consumer.auto-offset-reset=earliest
# Force Kafka to send messages immediately
spring.kafka.producer.properties.linger.ms=0

# --- Real SMTP Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tushargahtori26@gmail.com
# This is NOT your login password, but a 16-character 'App Password' from Google
spring.mail.password=vqfxxwnkfdazehas

# Security handshake settings
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true